[bdist_wheel]
# Tag the produced wheel as abi3 so pip / package_command.dart install it on
# any Python 3.12+ minor version. Keep in lockstep with Py_LIMITED_API in
# dart_bridge.c and the py_limited_api flag in setup.py.
py_limited_api = cp312
